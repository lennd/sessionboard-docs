---
interface Props {
  title?: string;
  href?: string;
  icon?: string;
  horizontal?: boolean;
}
const { title, href } = Astro.props;
const Tag = href ? 'a' : 'div';
---

<Tag class="sb-card" href={href}>
  {title && <p class="sb-card-title">{title}</p>}
  <slot />
</Tag>
