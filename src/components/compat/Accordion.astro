---
interface Props {
  title?: string;
  defaultOpen?: boolean;
}
const { title, defaultOpen = false } = Astro.props;
---

<details class="sb-accordion" open={defaultOpen}>
  <summary>{title}</summary>
  <div class="sb-accordion-body"><slot /></div>
</details>
